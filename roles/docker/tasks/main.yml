---
# tasks file for docker
- name: remove podman
  ansible.builtin.package:
    name:
      - podman
      - podman-docker
    state: absent

- name: install docker
  ansible.builtin.package:
    name:
      - docker-distribution
      - docker-compose